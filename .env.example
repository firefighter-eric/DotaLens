# Optional same-origin reverse-proxy path, for example /api/opendota.
# Leave unset to use https://api.opendota.com/api directly. Other origins are
# rejected at build time because the production Content Security Policy blocks them.
VITE_OPENDOTA_API_BASE=

# Optional immutable release identifier used by an injected host error reporter.
VITE_APP_RELEASE=
